{
  "name": "@fast-consig/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts,.tsx",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@fast-consig/config": "workspace:*",
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.4.5",
    "@types/node": "^20.12.0"
  }
}
